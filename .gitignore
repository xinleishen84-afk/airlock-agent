# 构建产物 / build artifacts
/airlock
/airlock-agent
*.exe
*.test
*.out

# 覆盖率报告 / coverage
coverage.out
coverage.html

# 本地密钥挂载点——绝不能提交
# Local secret mounts — must never be committed
/secrets/
*.pem
*.key

# 编辑器与系统 / editors and OS
.DS_Store
.idea/
.vscode/
*.swp
